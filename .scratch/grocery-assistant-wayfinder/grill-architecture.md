## Question

Given the provider, data, UI, and observability findings, where should the deep module seams and dependency directions be drawn so milestone one stays small while milestone two can add a custom OpenAI `LanguageModelExecutor` without rewriting the app? Decide the public interfaces for catalog and household storage, model tools, disclosure policy, orchestration, provider adapters, tracing, demo-data reset, and SwiftUI presentation; also decide which pieces belong in local Swift packages versus the app target.
