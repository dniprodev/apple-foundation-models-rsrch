[project]
name = "grocery-reference-dataset-builder"
version = "0.1.0"
description = "Deterministic SQLite Reference Dataset builder for the Grocery Shopping Assistant"
requires-python = ">=3.11"
dependencies = [
    "duckdb==1.5.5",
]
